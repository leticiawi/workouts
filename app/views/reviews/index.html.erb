<div class="container" id="select">
  <div class="container border-bottom">
    <h3>Reviews</h3>
  </div>
    <div class="row">
      <% @trainning.reviews.each do |review| %>
        <%= review.rating %>
        <%= review.content %>
        <%= review.user.name %>
      <% end %>
    </div>
</div>
